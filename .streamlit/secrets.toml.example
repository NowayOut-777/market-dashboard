# Copy this file to secrets.toml and fill in real values.
# secrets.toml is gitignored — never commit it.

FRED_API_KEY = "your_fred_api_key_here"

# Optional: set a password to gate access to the deployed dashboard.
# Leave empty or remove the line for no password.
APP_PASSWORD = ""
